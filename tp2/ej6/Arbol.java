package ej6;

public class Arbol {
	/*	
	 ARBOL CON NODOS
	 
	  -otra clase:
	 	Nodo {
	 		Object info;
	 		Nodo izq;
	 		Nodo der;
	 	}
	 
	 public class Arbol  {
	 	Nodo raiz;
	 }
	 
	 private boolean hasElem(Node raizActual, Object o) {
	 	if (raizActual != null) {
	 		if (raizActual.getInfo().equals(o)) {
	 			return true;
	 		}
	 	}else if (raizActual.getInfo() < o) {
	 		return hasElem(raizActual.izq, o);
	 	}else {
	 		return hasElem(raizActual.der, o);
	 	}
	 }
	 
	 public boolean hasElem(Object o) {
	 	return hasElem(raiz, o);
	 }
	 
	*/ 
}
